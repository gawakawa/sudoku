---
paths: "deno.json, package.json"
---

# Deno Dependency Management

- Use `deno.json` for dependencies, not `package.json`
- Use `deno add <package>` to add dependencies
- Use `deno remove <package>` to remove dependencies
- Use `deno outdated` to check for outdated dependencies
- Use `deno update` to update dependencies

---
paths: "**/*.ts, **/*.tsx"
---

# TypeScript Rules

- Use arrow functions, not `function` declarations
- Use `const` only, not `let`
- Always write return types for functions
- Use `map`, `reduce`, etc. instead of `for` loops
